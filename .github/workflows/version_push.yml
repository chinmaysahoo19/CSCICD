on:
  workflow_dispatch:
env:
  INCREMENT_TYPE: major
jobs:
  print-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          cd node-hello
          yarn install
          ./git-utils.sh version-push
